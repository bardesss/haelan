# Local release signing. Copy to .env and fill in: .env is git-ignored and never committed.
# CI passes the same four values as environment variables instead, which always win.
HAELAN_KEYSTORE_BASE64=
HAELAN_KEYSTORE_PASSWORD=
HAELAN_KEY_ALIAS=
HAELAN_KEY_PASSWORD=
